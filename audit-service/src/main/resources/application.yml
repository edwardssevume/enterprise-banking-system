spring:
  application:
    name: audit-service

  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://127.0.0.1:8888}"

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus