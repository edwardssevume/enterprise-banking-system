spring:
  application:
    name: api-gateway

  config:
    import: "optional:"

  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false