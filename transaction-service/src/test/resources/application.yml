spring:
  application:
    name: transaction-service

  cloud:
    config:
      enabled: false

  datasource:
    url: jdbc:h2:mem:transaction_test_db;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

  kafka:
    bootstrap-servers: 127.0.0.1:9092
    consumer:
      group-id: transaction-service-test-group
      auto-offset-reset: earliest

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

security:
  jwt:
    secret: "VGhpc0lzQVRlc3RTZWNyZXRLZXlGb3JKV1RUZXN0aW5nMTIzNDU2"
    issuer: "enterprise-banking-auth-service"

internal:
  service:
    secret: "test-internal-service-secret"